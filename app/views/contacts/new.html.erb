<div class="container">
  <div class="row">
    <h1 class="text-center">Contact Form</h1>
    <p class="text-center">Noticed something wrong with 
    the site?  Please use this form to inform us, and we will get back to you 
    as soon as possible!</p>
    <div class="col-md-4 col-md-offset-4">
      <div class="well contact-well">
        <%= form_for @contact do |f| %>
        
        <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, class: 'form-control' %>
          </div>
          
          <div class="form-group">
            <%= f.label :email %>
            <%= f.text_field :email, class: 'form-control' %>
          </div>
          
          <div class="form-group">
            <%= f.label :comments %>
            <%= f.text_area :comments, class: 'form-control' %>
          </div>
          
          <%= f.submit 'Submit', class: 'btn btn-default' %>
        
        <% end %>
      </div>
      <% if user_signed_in? %>
      <div class="row row-line-break">
        <hr class="line-break"/>
      </div>
      
      <h1 class="text-center">Send in a Report</h1>
      <p class="text-center">Please send in a report if you see a pet that shouldn't 
      be listed (yours or someone else), something innapropriate, 
      or if you have any issues submitting a pet.</p>
      <br/>
      <%= link_to "Report Form", new_report_path %>
      <% end %>
      <br/>
    </div>
  </div>
</div>
 <div class="text-center container-fluid">
  <div class="divider">
  <div class="center">
    <%= image_tag("alienaisha.gif") %>
  </div>
  </div>
  <div class="ad">
    Ads go here
  </div>
</div>