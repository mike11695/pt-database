<div class="container">
  <div class="row">
    <h1 class="text-center">Report Form</h1>
    <p class="text-center">Please use this form to report anything you feel is wrong and an admin will get to it!</p>
    <p class="text-center"><b>NOTE: </b>If you are reporting a pet, PLEASE include the name of the pet in your report.</p>
    <div class="col-md-4 col-md-offset-4">
      <div class="well contact-well">
        <%= form_for @report do |f| %>
        
        <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, :value => current_user.username, class: 'form-control' %>
          </div>
          
          <div class="form-group">
            <%= f.label :issue %><br/>
            <%= f.select :issue, ['Listed Pet - Should not be listed', 
            'Innappropriate Content', 'Harassment', 'Site Errors', 'Pet Edits', 'False Information', 'Other'], class: 'form-control' %>
          </div>
          
          <div class="form-group">
            <%= f.label :body %>:<i> Summarize your issue here</i>
            <%= f.text_area :body, class: 'form-control' %>
          </div>
          
          <%= f.submit 'Submit', class: 'btn btn-default' %>
        
        <% end %>
      </div>
    </div>
  </div>
</div>